int missingNumber(int* nums, int numsSize) {
    int esum = (numsSize * (numsSize + 1)) / 2;
    int asum = 0;
    
    for (int i = 0; i < numsSize; i++) {
        asum += nums[i];
    }
    
    return esum - asum;
}
    
